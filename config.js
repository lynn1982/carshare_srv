/**
 * config
 */

var config = {
    
    host: 'localhost',

    database: 'car_share',

    db_username: 'root',

    db_passwd: '111111',

    redis_host: '127.0.0.1',
    redis_port: 6379,
    redis_db: 0,
    redis_passwd: '',

};

module.exports = config;
