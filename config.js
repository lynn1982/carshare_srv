/**
 * config
 */

var config = {
    host: 'localhost',
    database: 'cheweidb',
    db_username: 'chewei',
    db_passwd: '123456',
    redis_host: '127.0.0.1',
    redis_port: 6379,
    redis_db: 0,
    redis_passwd: '',
    session_secret: 'chewei'
};

module.exports = config;
